# Example Ultralytics dataset config for fine-tuning a drone detector.
# Replace the paths and confirm the class name matches your annotations.

path: /absolute/path/to/drone-dataset
train: images/train
val: images/valid
test: images/test

names:
  0: drone
